export function getFilterData(array: any[], filter: any) {
  // const { coats, colors, gender } = filter ;
  // const result = []
  // array.forEach((item):any => {
  //     if ( coats !== 'all' ) ;
  //     if ( colors !== 'all' ) ;
  //     if ( gender !== 'all' )
  // })
}

export function isArrayHasItem(array: any[]) {
  return array.length !== 0;
}
